language: python
cache: pip
dist: bionic

matrix:
    fast_finish: true

install:
  - pip install tox tox-venv tox-travis pip wheel codecov -U

script:
  - tox

after_success:
  - codecov -e TOX_ENV

branches:
  only:
    - master
